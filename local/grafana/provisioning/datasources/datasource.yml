apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus      # matches the dashboard's datasource uid (same as prod)
    access: proxy
    url: http://prometheus:9090
    isDefault: true
